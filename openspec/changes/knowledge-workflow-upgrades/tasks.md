## 1. 数据模型与迁移（P0）

- [x] 1.1 [P0] 在 `prisma/schema.prisma` 新增 `VideoTimestampNote`、`VideoTag`、`VideoTagRelation` 模型并补齐索引与关联
- [x] 1.2 [P0] 扩展 `Notebook` 模型新增 `mode` 与 `rule` 字段并保证默认兼容 manual 模式
- [x] 1.3 [P0] 生成并校验 Prisma 迁移，完成本地迁移与回滚脚本说明

## 2. 时间戳笔记 API（P0）

- [x] 2.1 [P0] 新增 `POST /api/videos/[bvid]/notes`，支持创建时间戳笔记并做参数校验
- [x] 2.2 [P0] 新增 `GET /api/videos/[bvid]/notes`，按时间升序返回当前用户笔记
- [x] 2.3 [P0] 新增 `PATCH/DELETE /api/videos/[bvid]/notes/[noteId]`，支持编辑和删除并校验归属

## 3. 标签与智能合集 API（P0）

- [x] 3.1 [P0] 新增标签管理接口（创建、重命名、删除、列表）并实现用户隔离
- [x] 3.2 [P0] 新增视频标签绑定接口（添加/移除标签）
- [x] 3.3 [P0] 扩展 `/api/notebooks` 创建与查询逻辑，支持 `mode=smart` 与 `rule` 入参
- [x] 3.4 [P0] 为智能合集实现规则查询器（关键词、UP主、标签 AND 逻辑）并接入 notebook 详情页数据源

## 4. 全局搜索能力（P1）

- [x] 4.1 [P1] 新增跨视频字幕全文检索接口（按 `user_videos` 做用户过滤，支持分页）
- [x] 4.2 [P1] 扩展现有 `/api/knowledge/search` 返回统一字段结构并补充来源标识
- [x] 4.3 [P1] 新增聚合搜索接口（全文 + 语义）与来源/标签过滤参数

## 5. 模板化输出能力（P1）

- [x] 5.1 [P1] 新增内置模板配置（`ppt-outline`、`wechat-article`、`xiaohongshu-post`）
- [x] 5.2 [P1] 新增 `POST /api/templates/generate`，复用流式 LLM 输出模板生成结果
- [x] 5.3 [P1] 为模板生成增加上下文校验（摘要/字幕缺失时报错）与统一错误码

## 6. 前端集成（P1）

- [x] 6.1 [P1] 在分析页字幕区增加“记笔记”入口、笔记列表面板与时间戳跳转交互
- [x] 6.2 [P1] 在分析页/历史页增加标签编辑交互与智能合集入口
- [x] 6.3 [P1] 在搜索页接入聚合搜索接口并增加来源过滤、分页与结果分组展示
- [x] 6.4 [P1] 在分析页增加模板输出面板（模板选择、流式展示、复制结果）

## 7. 验证与发布（P2）

- [x] 7.1 [P2] 为新增 API 补充关键用例（鉴权、归属校验、参数校验、规则查询）
- [x] 7.2 [P2] 补充端到端手工验收清单（笔记->合集->搜索->模板输出全链路）
- [x] 7.3 [P2] 更新 `docs/` 发布文档，包含迁移步骤、开关策略、回滚流程
