# 视频转写速度优化技术方案

## 一、现状分析

### 1.1 当前转写流程

```
用户请求分析视频
    │
    ▼
获取视频信息 + B站 CC 字幕
    │
    ├── 有 CC 字幕 → 直接返回（秒级）✅
    │
    └── 无 CC 字幕 → 进入转写流程
            │
            ▼
        下载音频（M4A，通过 B站 DASH API）
            │ ~10-30秒（取决于视频时长和网络）
            ▼
        语音转写（videocaptioner transcribe --asr bijian）
            │ ~3-10分钟（CPU 纯转，1个任务独占）
            ▼
        返回 SRT 字幕 → 存入数据库 Video 表
```

### 1.2 当前环境约束

| 资源 | 规格 | 说明 |
|------|------|------|
| CPU | 2核 | 仅支持轻量并发 |
| 内存 | 1.6GB（可用 864MB） | 极紧张，限制模型加载 |
| GPU | **无** | 转写只能走 CPU |
| 磁盘 | 28GB /tmp 可用 | 音频文件缓存充足 |
| PostgreSQL | 与 Next.js 共用 1.6GB | 内存压力大 |
| TRANSCRIBE_MAX_CONCURRENCY | 1 | 强制串行 |

### 1.3 当前转写瓶颈

**核心矛盾：CPU 无 GPU，Whisper/ bijian 模型只能 CPU 推理，速度极慢。**

```
10分钟视频 ≈ 600秒音轨
Whisper base 模型 CPU: ~0.5x realtime → 20分钟
Whisper small 模型 CPU: ~0.3x realtime → 33分钟
bijian 模型（bilingual）: 无明确数据，CPU 下预计 5-15分钟
```

### 1.4 已有优化：数据库缓存

Video 表已存储 `subtitleText` 和 `subtitleSource`，同一视频重复分析不会重新转写。但：
- 用户首次分析冷门视频仍需等待
- 新用户首次使用任何视频都需要等待

---

## 二、方案对比

### 方案 A：云 ASR API 替代本地转写

**思路**：用阿里云/讯飞/百度等国内 ASR API 替换本地 Whisper，服务器只负责音频下载，推理全部在云端。

| 维度 | 评估 |
|------|------|
| **速度** | 实时~1分钟（API 通常 1/3 时长内返回） |
| **成本** | 约 ¥0.1-0.3/分钟，按量计费 |
| **准确率** | 中文优化好，准确率高于 Whisper base/small |
| **部署复杂度** | 低：改一个 API 地址即可 |
| **依赖风险** | 中：依赖第三方服务，API 限流/涨价风险 |
| **隐私** | 中：音频数据送第三方，需用户同意 |
| **服务器负载** | 极低：仅下载+透传 |

**推荐配置**：

| 服务商 | 产品 | 价格（参考） | 中文支持 |
|--------|------|-------------|---------|
| 阿里云 | 智能语音交互 ASR | ¥0.12/分钟 | ⭐⭐⭐⭐ |
| 讯飞 | 语音识别（中文） | ¥0.18/分钟 | ⭐⭐⭐⭐⭐ |
| 百度智能云 | 短语音识别 | ¥0.04/分钟 | ⭐⭐⭐⭐ |
| 腾讯云 | ASR | ¥0.13/分钟 | ⭐⭐⭐⭐ |

**改动点**：
```typescript
// lib/bilibili.ts 或新建 lib/cloud-asr.ts
export async function transcribeWithCloudAPI(audioPath: string): Promise<string> {
  // 1. 用 curl 上传音频到 ASR API
  // 2. 获取结果（通常是 SRT 或带时间戳的 JSON）
  // 3. 转为统一 SRT 格式返回
}
```

**风险点**：
- 需在 .env 配置 API Key
- 阿里云/讯飞对个人用户有调用频率限制
- 百度 ASR 价格最低但偶尔有嗓点

---

### 方案 B：升级服务器 GPU

**思路**：将转写服务迁移到带 GPU 的服务器（或新增 GPU 实例），本地运行 Whisper。

| 维度 | 评估 |
|------|------|
| **速度** | GPU Whisper large: ~100x realtime，10分钟视频 ~6秒 |
| **成本** | GPU 云服务器 ¥0.8-2/小时（按量），¥300-800/月（包月） |
| **准确率** | Whisper large 准确率最高 |
| **部署复杂度** | 高：需要新服务器 + CUDA 环境 + 模型下载 |
| **稳定性** | 高：完全自主可控 |
| **适用场景** | 转写量大（每天 >30分钟）、长期运营 |

**推荐配置**：

| 方案 | 实例 | 价格 |
|------|------|------|
| 按量 GPU | 阿里云 GPU gn6v（V100 16GB） | ¥1.88/小时 |
| 包月 GPU | 阿里云 gn6v | ¥806/月 |
| 轻量 GPU | 腾讯云 GPU 服务器 LAMD5（V100） | ¥699/月 |
| 竞价实例 | 阿里云 GPU 竞价实例 | ¥0.5-1/小时（可中断） |

**改动点**：
- 新增 GPU 服务器部署转写服务（独立进程或 Docker）
- Next.js 通过 HTTP 调用转写服务
- 模型：faster-whisper + Whisper large-v3 + int8 量化

---

### 方案 C：faster-whisper + 量化优化（当前服务器）

**思路**：在现有 1.6GB 服务器上，用 faster-whisper（纯 Python/C++）替代 videocaptioner，选小模型 + int8 量化，在 CPU 上压榨极限性能。

| 维度 | 评估 |
|------|------|
| **速度** | faster-whisper 比原生 Whisper 快 2-4 倍（CPU） |
| **成本** | ¥0（利用现有资源） |
| **准确率** | 取决于模型大小，small+int8 与 base 相当 |
| **内存占用** | int8 量化大幅降低内存，1.6GB 可运行 small 模型 |
| **部署复杂度** | 中：安装 faster-whisper + 下载模型文件 |
| **风险** | 内存仍紧张（Next.js + PostgreSQL + 转写模型），可能 OOM |

**faster-whisper 模型对比**：

| 模型 | 参数量 | 内存占用 | 速度（CPU） | 准确率 |
|------|--------|---------|-------------|--------|
| tiny | 39M | ~150MB | ~30x realtime | 较差 |
| base | 74M | ~300MB | ~15x realtime | 中等 |
| small | 244M | ~900MB | ~8x realtime | 良好 |
| medium | 769M | ~3GB+ | ~3x realtime | 较好 |

> small + int8 量化 ≈ 600MB，1.6GB 服务器勉强能跑，但内存紧张风险高。

---

### 方案 D：并发转写 + 队列优化

**思路**：不改变转写引擎，而是优化任务调度——提高并发上限 + SSE 进度反馈 + 后台预转写。

| 维度 | 评估 |
|------|------|
| **速度** | 整体吞吐提升，但单个任务不变 |
| **成本** | ¥0 |
| **部署复杂度** | 低 |
| **效果** | 有限，CPU 仍是瓶颈 |

**具体措施**：
1. 提高 `TRANSCRIBE_MAX_CONCURRENCY`（当前 1 → 2）
2. 用户体验优化：SSE 心跳反馈（当前已有）、取消功能
3. **预转写热门视频**：B站热榜视频提前转写，存入数据库
4. **异步转写**：用户先拿到音频链接，后台异步转写完成后通知

---

### 方案 E：缓存 + P2P 共享转写结果

**思路**：利用已转写过的视频数据，多用户共享结果，减少重复转写。

| 维度 | 评估 |
|------|------|
| **速度** | 命中缓存时秒级返回 |
| **成本** | ¥0 |
| **部署复杂度** | 低（Video 表已支持） |
| **覆盖率** | 取决于视频热门程度，冷门视频仍需转写 |

**已有基础**：
- Video 表已通过 `bvid` 唯一索引存储 `subtitleText`
- 同一视频被任何用户分析过，后续用户直接读缓存

**可增强**：
- 添加视频热度权重，优先预转写热榜视频
- 建立视频字幕共享机制（类似字幕组）

---

## 三、方案选择建议

### 根据用户规模和预算选择

| 场景 | 用户量 | 月预算 | 推荐方案 |
|------|--------|--------|---------|
| 初期验证 | 1-10人 | ¥0 | 方案 C（faster-whisper int8） |
| 早期增长 | 10-100人 | < ¥200 | 方案 A（云 ASR API） |
| 产品成型 | 100-1000人 | ¥200-1000 | 方案 A + E（云 API + 缓存） |
| 中期规模 | 1000人+ | > ¥1000 | 方案 B（GPU 独立服务） |

**当前情况**：3个用户，月预算未知，建议 **方案 A 为主 + 方案 E 为辅**。

### 组合方案（推荐）

```
短期（立即可做）：
  方案 A：接入阿里云 ASR，替换 videocaptioner 转写
  方案 E：确认缓存机制已生效（bvid 唯一索引已存在）

中期（1-2周）：
  方案 D：SSE 进度优化 + 预转写热榜视频

长期（如用户量涨到 100+）：
  方案 B：GPU 独立转写服务
```

---

## 四、方案 A 详细设计（推荐立即实施）

### 4.1 阿里云 ASR 接入

**产品**：阿里云智能语音交互 → 短语音识别（实时），或语音异步识别（长音频）

**价格**：¥0.12/分钟，按量计费

**优势**：
- 中文 ASR 优化，B站视频场景准确率高
- 按调用量计费，小用户量下成本极低（3用户 × 每天10分钟 × ¥0.12 = ¥9/月）
- RESTful API，接入简单

**接入方式**：语音流式转写（直接发送本地音频文件）

```typescript
// src/lib/aliyun-asr.ts
// 阿里云语音识别 API 调用示例
```

### 4.2 改动范围

| 文件 | 改动 |
|------|------|
| `src/lib/bilibili.ts` | `downloadAudioViaApi` 不变 |
| 新建 `src/lib/aliyun-asr.ts` | 接入阿里云 ASR API |
| `src/lib/videocaptioner.ts` | 保留 downloadAudio（备用），新增 transcribeAPI 函数 |
| `src/app/api/transcribe/route.ts` | 切换转写引擎 |
| `.env.local` | 新增 `ALIYUN_ASR_KEY` 等配置 |

### 4.3 降级策略

```
优先：阿里云 ASR API
    ↓ 失败（限流/网络/账户余额）
降级：videocaptioner --asr bijian（当前方案）
    ↓ 失败（服务器资源不足）
降级：返回错误，提示用户"该视频暂无法转写"
```

---

## 五、方案 B 详细设计（长期）

### 5.1 架构设计

```
用户浏览器
    │
    ▼
www.afai.asia (Next.js)
    │ HTTP 请求转写任务
    ▼
GPU 转写服务器 (独立部署)
    │ CUDA + faster-whisper
    ▼
Whisper large-v3 (int8 量化)
    │ ~6秒/10分钟视频
    ▼
返回 SRT 结果
```

### 5.2 腾讯云 GPU 服务器（推荐）

- **产品**：腾讯云 GPU 计算型 G-HAT（V100 32GB 或 A10 16GB）
- **价格**：¥699/月起
- **网络**：内网与上海/广州 Region，通过公网 API 调用

### 5.3 模型配置

```python
from faster_whisper import WhisperModel

model = WhisperModel(
    "large-v3",
    device="cuda",        # GPU 加速
    compute_type="int8",   # 精度换速度
)
```

---

## 六、用户体验优化（与引擎无关）

无论选择哪种引擎，以下优化都应同步推进：

### 6.1 SSE 进度优化

当前心跳间隔 15秒，用户感知差。建议：

```
0秒    → "正在下载音频..."
10秒  → "正在语音转写，请稍候..."
25秒  → "转写进度: 35%"
40秒  → "转写进度: 70%"
55秒  → "转写完成，正在处理..."
```

### 6.2 异步 + 通知

用户发起转写后：
1. 立即返回「排队中」状态，返回任务 ID
2. 前端展示进度条（5分钟预估）
3. 转写完成后 WebSocket / SSE 通知用户
4. 用户可关闭页面，后续打开仍能查看结果

### 6.3 预转写热榜

```
定时任务（每天一次）：
  1. 获取 B站综合榜 Top 50
  2. 检查数据库中是否已有 subtitleText
  3. 未有的视频 → 后台自动转写
  4. 用户访问时直接命中缓存
```

---

## 七、总结

| 优先级 | 方案 | 工作量 | 效果 | 建议 |
|--------|------|--------|------|------|
| **P0** | 方案 A：云 ASR API | 1-2天 | 转写时间 10分钟→1分钟 | 立即实施 |
| **P0** | 方案 E：缓存机制验证 | 0.5天 | 命中缓存秒级返回 | 立即验证 |
| **P1** | 方案 D：并发 + 预转写 | 2-3天 | 整体吞吐提升 | 短期推进 |
| **P2** | 方案 B：GPU 独立服务 | 1周+ | 极致性能 | 用户量涨到100+后考虑 |
| **P3** | 方案 C：faster-whisper 优化 | 2天 | 在现有硬件上压榨性能 | 作为降级方案 |

**行动项**：
1. 申请阿里云 ASR API Key（个人用户即可）
2. 验证 Video 表 bvid 索引缓存命中逻辑
3. 搭建阿里云 ASR 测试环境，对比准确率和速度
4. 设计异步转写 + WebSocket 通知方案
